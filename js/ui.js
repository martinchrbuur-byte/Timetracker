window.UI={screen(el,html){el.innerHTML=html},toast(msg){const t=document.querySelector('#toast');t.textContent=msg;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),1800)},modal(title,body,buttons=''){const d=document.createElement('div');d.className='modal';d.innerHTML=`<div class="panel"><h2>${title}</h2><p>${body}</p><div class="menu-actions">${buttons}</div></div>`;d.onclick=e=>{if(e.target===d)d.remove()};document.body.append(d);return d}};
